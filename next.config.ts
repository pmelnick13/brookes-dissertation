// this is ready for any next.js settings the project needs later
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // extra project settings can go in here later
};

export default nextConfig;
